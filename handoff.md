# 交接檔（handoff.md）

> 任何 Agent、任何電腦接手前**必讀**；收工時**必更新**。本檔只放交接必需的精簡資訊，詳細脈絡放 `obsidian/工作記錄.md`。

## ⏯️ 目前做到哪

完成「流標重新排拍」的兩個方向並上線：`publish_new.py` 改為**同案號就地更新**（代號與 tinyurl 不變），
`purge_expired.py` 保留期 7→30 天。順帶修好站上 24 筆「明明還在拍卻被隱藏」的過期物件（含 24 筆語音）。
另把 Google Drive 上重複的第二份 clone 統一刪除。

## 🚦 目前狀態

- **可運行**。線上 385 筆物件，voice／card／hero 缺 0，BASE_ITEMS 與資料夾完全一致（385 = 385）。
- 工作區乾淨，與 `origin/main` 同步。
- 沒有做一半的東西。

## ➡️ 下一步

1. **2026-08-28**：`python purge_expired.py --dry-run` →確認後 `python purge_expired.py --push`。
   預設保留期已是 30 天，不必再加 `--days`。
2. **下次上架**：驗證就地更新在「有新物件＋有更新」混合情境下的表現。
   目前只實測過純更新（24 筆更新、0 筆新增）。更新後**務必補跑語音與 `burn_price.ps1`**——
   `publish_new.py` 會刪掉更新物件的 `voice.mp3`／`card.jpg`／`hero.jpg` 等待重生。
3. （可選）架構候選 B：頁面模板單一真理源，見 `agents.md` 路線圖。

## ⚠️ 注意事項

- **`publish_new.py` 不在本 repo**，在 `G:\我的雲端硬碟\ai agent\法拍 104\`，
  其 `REPO` 常數**寫死指向本資料夾**。搬動或改名本資料夾時必須同步修改，否則產線會寫到錯的地方。
- **保留期與就地更新必須成套**。若把保留期改回 7 天又用 `--no-update`，會出現最糟組合：
  舊物件還在（案號比對到）→ 新拍別被當重複跳過 → 物件頁停在過期拍別 → 被 `delist.js` 隱藏
  → **還在拍的物件反而從網站上消失**。
- **爬蟲的開標時間常是 `..` 之類的雜訊**。就地更新已處理（能解析才採用，否則清空交給 `delist.js` 以 17:00 保守起算），
  但新增物件路徑仍是原樣寫入，留意頁面出現 `..`。
- **`.gitignore` 的 `output/` 已改寫為 `output/*`**。git 不會進入被排除的「目錄」，
  寫成 `output/` 時 `!output/xxx` 例外規則不會生效。要再對單一檔案開例外請沿用 `output/*` 寫法。
- **`w356` 曾是產線母版的寫死來源**，但它只是一般法拍物件（開標日 2026-10-20），
  30 天保留期下 2026-11-19 會被自己刪掉。已改成不存在時自動退到現存代號最大的物件頁，不必再擔心。
- **只能有一份 clone**。2026-08-22 前有兩份，導致其中一份落後 61 個 commit 沒人發現。

## 🕐 最後更新

- 時間：2026-08-22 06:40
- 更新者：Claude Code (Opus 5) @ KEN-PC
- Git push：✅ 已推
