# 👑 法拍女王網頁發布工具 - 工作記錄

## 📅 2026-09-06 工作記錄：全域設定備份重建（原備份 repo 消失）

- **`D:\ganju-erp` 整個不見了**。收尾要跑 `pnpm agent:backup` 時發現。查證：
  不在 D: 根目錄（完整列了 25 個資料夾）、不在資源回收筒、C:／D:／X: 前三層搜不到。
  `D:\.pnpm-store` 還在、日期 2026-07-30，跟它當初搬到 D: 的時間對得上。
  **何時、為何消失查不出來**，超出我能查證的範圍。

- **損失盤點**：8/28 在那裡的三個 commit（`da7fae3`、`598af2e`、`ab69ddd`）
  **只 commit 沒 push**，所以連同 repo 一起沒了。但**正本都還在家目錄**，
  已逐項確認：`~/.claude/CLAUDE.md` 仍是 139 行合併版、關鍵句都在；
  6 個 skills、`AGENTS.md` 都在。**唯一真的損失**是我對
  `backup-agent-config.ps1` 加的 `scheduled-tasks` 那一行——而上架排程已改用
  Windows 工作排程器，那行也不再關鍵。真正的問題是**當下沒有任何備份**。

- **🔴 差點把全域設定推進公開 repo**。使用者指示「備份到 Queen-of-Auctions」，
  動手前先查了 repo 可見性：
  | repo | 未登入 API | 結論 |
  |------|-----------|------|
  | Queen-of-Auctions | 200，`private=False` | **公開** |
  | 104woo | 404 | 私有 |
  全域設定含「使用者身份與背景定義」（公司名、代管業／包租業範疇）與工作習慣。
  掃過沒有金鑰、密碼、身分證、電話，但推進公開 repo 等於永久公開，git 歷史難撤回。
  **提出後改備份到私有的 104woo。**
  教訓：**動手前先確認 repo 可見性**——「備份到某個 repo」這種指示，
  給指示的人未必把可見性算進去。

- **新備份機制**：`法拍 104\backup_agent_config.ps1` ＋ `agent-config/`
  - 沿用原設計：家目錄是正本、repo 是快照；`-Restore` 反向還原；**刻意不自動 commit**
  - 複製前掃描疑似金鑰值，掃到就中止。只認「看起來像值」的樣式——
    **一直誤報的檢查等於沒有檢查**
  - 12 個檔案已逐一核對與正本位元相同，commit `0cf1d16` 並**推上 GitHub**（這次有 push）

- **連帶修掉四處還指向舊位置的文件**：`handoff.md`、`docs/新電腦設定.md`、
  `G:\我的雲端硬碟\CLAUDE.md` 指標檔、以及 **shutdown skill**——
  收工技能本來寫死「提醒跑 `pnpm agent:backup`」，不改的話每次收工都會叫你跑一個不存在的指令。
  順手在 skill 裡補上「要 **commit + push**」，因為這次就是只 commit 沒 push 才全沒了。

- **下一步**：
  1. **查 `ganju-erp` 的下落**（可能搬走、或在另一台機器）。找回來的話這裡可以退役，
     或反過來留著當第二份。
  2. **9/9（三）09:07** 第一次自動執行後查 `LastTaskResult` 與 log。

## 📅 2026-09-05 工作記錄：排程改用 PowerShell 腳本 + Windows 工作排程器

- **Claude Code 的排程連續三次全部空轉**。8/28 建好後，08-29（六）、09-02（三）、09-05（六）
  都準時觸發，但每次都在**第 3 個指令 `git pull` 就停住**，13 秒內結束、什麼都沒做：
  沒有爬蟲輸出檔、沒有 commit、441 筆物件不變。
  - transcript 結束在 `git pull` 的 tool_use，**沒有對應的 tool_result**——這是「等待授權」
    的形狀，不是失敗的形狀
  - `git status` 每次都過，是因為唯讀指令自動放行，跟規則無關；它成功**沒有證明任何規則生效**
  - 9/5 那次檔案 392 KB（前兩次 62 KB）一度讓我以為走得比較遠，實際上工具呼叫數都是 3 個，
    檔案變大只是 context 附件變多。**看檔案大小判斷進度是錯的**

- **9/2 加的 `permissions.allow` 沒解決**。該次執行的 `command_permissions` 是 `allowedTools: []`。
  最可能的原因是**指令形狀**：agent 送出的是 `cd "..." && git pull --ff-only origin main`，
  而規則 `Bash(git pull:*)` 比對的是開頭——這條指令開頭是 `cd`。

- **決定：整條產線搬出 Claude Code**。回到第一性原理——
  爬蟲／publish_new／fix_city_labels／burn_price／apply_*／語音全是固定順序與參數，
  護欄是三個數字比較，commit 訊息可模板化，驗證是比對 HTTP 狀態碼。
  **這條產線根本不需要 LLM。** 當初放進 Claude Code 是因為 skill 寫在那裡（沿用），
  不是因為工作需要 agent（推導）。

- **新架構**：
  | 元件 | 位置 |
  |------|------|
  | 總控腳本 | `run_pipeline.ps1`（218 行，在 repo 內、有版控）|
  | 排程 | Windows 工作排程器 `QueenOfAuctions-Publish`，每週三、六 09:07 |
  | log | `output\pipeline-logs\<時間戳>.log`（`Start-Transcript`，gitignore）|
  | 統計 | `publish_new.py` 新增 `--stats-out`，寫 JSON 給腳本讀 |

- **`--stats-out` 為什麼要用檔案而不是解析 stdout**：統計是中文輸出，
  PowerShell 5.1 讀 python 的 UTF-8 容易亂碼，正規表達式比對不可靠。
  這台機器已經被編碼問題咬過太多次（`.ps1` 的 BOM、Python 寫檔的 CRLF），不再賭。

- **腳本的護欄與退出碼**（比 skill 版更嚴）：
  - 爬蟲 < 800、新物件 > 200、更新 > 200 → 中止不推
  - **工作區不乾淨直接中止**，不自行 commit 或丟棄使用者做到一半的東西
  - **實際筆數與 dry-run 不符也中止**（防止兩次讀到不同資料）
  - 退出碼：0 成功或無異動｜1 護欄觸發｜2 執行失敗｜3 工作區不乾淨
  - commit 主體排在語音之前——語音若失敗，前面的工作也已保住
  - 最後檢查每個物件資料夾的 index/voice/card/hero 是否齊全才算成功

- **實測**：
  - 髒工作區護欄 → exit 3，訊息明確，沒有擅自處理 ✅
  - `-DryRun -SkipCrawl` 全程 → exit 0，工作區仍乾淨（確認沒寫入）✅
  - log 與 stats.json 都有產出，且被 `output/*` 擋在版控外 ✅

- **舊排程已刪除**（3 次執行紀錄封存，`SKILL.md` 留著備查）。
  兩個並存的話，萬一舊的哪天真的跑起來就會重複執行。
  skill `auction-publish` 保留為**手動路徑**，你在場說「爬蟲上架」時用。

- **上次做到哪**：新架構已上線，下次自動執行 **2026-09-09（三）09:07**。
  上次爬蟲 8/28，目前 dry-run 顯示有 **3 筆待更新、0 筆新物件**（w595、w452、w451）。

- **下一步**：
  1. **9/9（三）09:07** 第一次自動執行後，用
     `Get-ScheduledTaskInfo -TaskName 'QueenOfAuctions-Publish'` 看 `LastTaskResult`，
     並查 `output\pipeline-logs\` 最新的 log。
  2. **2026-09-17 起**可跑 `purge_expired.py --dry-run`（最早到期 `w433`）。
     **腳本不碰這件事**，刪除仍要人確認。

## 📅 2026-08-28 工作記錄（收尾）：全域設定還原、換機文件、備份缺口

> 🔎 **找「換電腦怎麼重建」看這裡** → [docs/新電腦設定.md](../docs/新電腦設定.md)

- **發現全域 `CLAUDE.md` 在 2026-08-04 被換掉**。跑 `pnpm agent:backup` 時，
  備份 repo 出現一個我沒動過的 `M agent-config/claude/CLAUDE.md`。查下去發現：
  - 備份 repo 內是 8/3 的「全域工作方式」（71 行，只談第一性原理／金字塔原理／法規查證）
  - 家目錄現況是 8/4 的「全域設定 / Global Rules」（70 行，談歸檔／身份／三處同步／開工收工／語音／MCP）
  - 兩者**不是版本差異，是兩份不同的文件**；而且家目錄那份與 `G:\我的雲端硬碟\CLAUDE.md`
    **位元完全相同**（`cmp` 驗證），顯然是 G: 那份被複製過去覆蓋掉的
  - 代價：8/3 版那三節被壓縮成三行摘要，**遺失了最有價值的「會出錯的地方」**——
    「推導不能取代查證」的邊界、「營業稅是加值型 → 包租業應差額課稅」這個推理方向對
    但結論全錯的反例、「同名不同法」（第 32 條在營業稅法與租賃住宅條例都有但管不同的事）、
    「結論先行 ≠ 省略不確定」、「拿已知事實去測來源，一處條號錯就整份重驗」

- **處理：合併而非二選一**。兩份其實互補——8/3 談「怎麼想、怎麼說」，8/4 談「怎麼做」。
  以 8/4 的結構為底，第 3 節換成 8/3 的完整三節（降為 3.1～3.3），
  並補回 8/3 開頭的定位說明與 8/4 點名的官方資料庫。
  逐行比對確認 8/3 版零遺失、8/4 版只少掉被完整版取代的三行摘要。71 + 70 → **139 行**。
  已寫回家目錄並 commit 進備份 repo（`598af2e`）。

- **`G:\我的雲端硬碟\CLAUDE.md` 改成指標檔**（4348 → 1233 bytes）。
  它會以 project instructions 身分被載入，與家目錄那份重複。覆蓋前逐行確認它**沒有任何獨有內容**。
  指標檔裡把這次的來龍去脈寫進去，讓下次想「順手在這裡加條規則」的人先看到為什麼不該加。
  **同一份規則放兩個地方，遲早會再走偏一次**——這次就是。

- **補上備份缺口：`.claude\scheduled-tasks` 納入 `agent:backup`**。
  原本只快照 `AGENTS.md`、`CLAUDE.md`、`skills\`，所以換電腦時 skill 跟得走、
  **排程跟不走**。改 `D:\ganju-erp\scripts\backup-agent-config.ps1` 的 `$items` 加一列即可
  （腳本本來就把那裡設計成擴充點）。實測快照成功，commit `ab69ddd`。
  ⚠️ 那支 `.ps1` 含中文，是 **UTF-8 with BOM**，改寫時要用 `utf-8-sig` 讀寫保留 BOM。

- **新增 `docs/新電腦設定.md`**。把換機重建步驟寫成照著做就會動的文件，
  每一步都在 KEN-PC 實跑驗證過。三個最容易卡住的地方：
  1. **torch 必須 cu128**——RTX 5070 是 sm_120，裝成 CPU 版或 cu121 都不行
  2. **`publish_new.py` 的 `REPO` 寫死絕對路徑**——路徑不對會安靜地寫到不存在的地方
  3. **ffmpeg 靠 winget 路徑萬用字元找**——非 winget 安裝要改 `_FF_GLOB`

- **今日全域設定現況**：
  | 位置 | 內容 | 版控 |
  |------|------|------|
  | `~\.claude\CLAUDE.md` | 唯一真理源，139 行 | ✅ ganju-erp `598af2e` |
  | `~\.claude\skills\auction-publish\` | 上架產線 skill | ✅ ganju-erp `da7fae3` |
  | `~\.claude\scheduled-tasks\` | 每週三、六排程 | ✅ ganju-erp `ab69ddd` |
  | `G:\我的雲端硬碟\CLAUDE.md` | 指標檔 | ❌ 不在任何 repo（本來就是） |

## 📅 2026-08-28 工作記錄（下半場）：產線 skill 化並排程
- **把今天跑成功的產線寫成 skill `auction-publish`**（`C:\Users\ken\.claude\skills\auction-publish\SKILL.md`）。
  內容不只是指令清單，還把「為什麼」寫進去，避免後人踩同樣的坑：
  - 新增 vs 就地更新的分流邏輯，以及「更新會刪掉 voice/card/hero 等待重生」這個關鍵副作用
  - `--max-pages` 為何是 20 不是 60（60 頁多跑一小時、新物件 0 筆）
  - `fix_city_labels.py` 為何必須早於語音（它會改 `narrText` 講稿）
  - 保留期 30 天為何不能改回 7 天
  - 背景執行別接 `| tail -N`、Python 寫清單餵 curl 要先去掉 CR

- **排程 `auction-publish-wed-sat`：每週三、六 09:07 自動執行並推上線**。
  使用者選擇「自動跑完並推上線」而非「停下來等確認」。為此在 skill 裡加了**護欄**：
  | 條件 | 理由 |
  |------|------|
  | 爬蟲總筆數 < 800 | 正常 1300～1450 筆，太少代表網站改版或網路異常 |
  | 新物件 > 200 筆 | 正常 50～180 筆，暴增通常是案號比對壞了 |
  | 更新 > 200 筆 | 同上 |
  任一觸發就中止不推、只回報。工作區不乾淨也會停下來問人，不會自行 commit 或丟棄。
  `purge_expired.py` 排程只跑 `--dry-run` 回報，**刪除一律要人確認**。

- **為什麼排程必須在本機、不能用雲端**：語音要本機 RTX 5070（約 28 秒/筆），
  雲端 GitHub Actions 是 CPU、一筆要 20 分鐘且有 6 小時上限。
  `mcp__scheduled-tasks` 存在 `C:\Users\ken\.claude\scheduled-tasks\` 並在本機執行，符合需求。
  **前提是電腦開著且 Claude Code 有開**，關著會在下次啟動時補跑。

- **⚠️ 待辦**：skill 與排程都在 `C:\Users\ken\.claude\`，**不在任何 repo 裡**，換電腦不會跟著走。
  要跑 `D:\ganju-erp` 的 `pnpm agent:backup` 把快照同步進 `agent-config/` 並 commit。

- **踩到的坑（第三次同一個）**：用 heredoc 寫 Python 時反斜線會被吃掉。
  這次是把 `C:\\Users` 寫進字串，被吃成 `C:\Users`，`\U` 被 Python 當成
  unicode escape 而語法錯誤。解法一律用 `chr(92)` 組出反斜線，不要在 heredoc 裡寫字面反斜線。

## 📅 2026-08-28 工作記錄
- **今日工作內容**：跑完一輪完整爬蟲上架。這是「同案號就地更新」上線後第一次跑
  **新增＋更新的混合情境**（8/22 那次只驗證到純更新 24 筆、新增 0 筆）。
  - 爬蟲：嘉義縣／台南市／高雄市／屏東縣，20 頁，取得 **1368 筆待標**
  - 分流結果：**新物件 56 筆**（`w767`–`w822`）＋ **就地更新 55 筆**，總覽 385 → **441 筆**
  - 產線依 CLAUDE.md 順序跑完，三個獨立步驟都命中 111 筆（56 新 + 55 更新）：
    `publish_new.py` → `fix_city_labels.py`（修 w806／w814「嘉市」，同步 4 頁導覽名稱）
    → `burn_price.ps1` → `apply_price_images.ps1` → `apply_swipe_nav.py` → `local_voice_batch.py`
  - 語音 **111/111**，52 分鐘、平均 28 秒/筆、零失敗

- **核心驗收：55 筆更新的 tinyurl 全數存活**
  - 導向正確 **55/55**（每個短網址仍指向原本的 wNNN）
  - 目的地頁面 **55/55 回傳 200**（頁面確實還在，不是導向到已刪除的資料夾）
  - 抽驗 5 筆確認線上顯示的已是新拍別（w323→9/09、w763→9/08、w444→9/15、w684→9/08、w119→11/24）
  - **對照 2026-08-21 那批（舊邏輯）**：174 筆「新物件」裡有 124 筆其實是重新排拍，
    每筆都換新代號與新短網址，舊連結全數失效。本次 0 筆失效。

- **55 筆更新的組成**（跑之前用 dry-run 分類過，確認不是無意義的重跑）：
  - 拍別變動 **34 筆**（流標重新排拍，底價同步調降）
  - 只有開標日變 **16 筆**（同拍別改期）
  - 只有開標時間變 **5 筆**：w684／w698／w729／w730 由爬蟲雜訊 `..`、w395 由空值
    → 補為真實時間。屬於**修好壞資料**，補上後 `delist.js` 不必再用 17:00 保守推估
  - 只有底價變 0 筆
  - **其中 48 筆的站上舊資料已過期、正被 `delist.js` 隱藏**——客戶在總覽上看不到，
    但實際都還在拍。這 48 筆現在都回到總覽上了。

- **踩到的坑**：
  1. **驗證 tinyurl 時一度得到「0/55 全部失敗」**。0/55 這種結果比較像量測問題而不是連結真的全壞，
     所以先單筆手動查——`curl -D -` 顯示 301 與正確的 Location，連結是好的。
     真正原因是我的對照表用 Python 在 Windows 寫檔，預設換行是 `\r\n`，
     每個網址尾端多了一個 CR 字元導致 curl 請求失敗。`tr -d` 掉 CR 後重跑就是 55/55。
     **教訓：Python 寫出的清單要餵給 shell 工具時，開檔要指定 `newline='\n'`，或事後用 `tr -d` 去掉 CR。**
  2. 用 `run_in_background` 跑長時間腳本時**不要接 `| tail -N`**——tail 會緩衝到程序結束，
     中途完全看不到進度。這次靠直接數檔案系統上的 `voice.mp3` 才看得出進度。

- **上次做到哪**：
  - 上架與驗證全部完成並推上線（commit `42c7ad3`）。
  - 線上 441 筆與本地一致；`index.html`／`voice.mp3`／`card.jpg`／`hero.jpg` 缺 0；嘉市殘留 0。
  - 目前在架 352／441 筆（其餘 89 筆已過開標時刻，被 `delist.js` 隱藏，屬正常）。

- **下一步**：
  1. **2026-09-17 起**跑 `python purge_expired.py --dry-run`。今天（8/28）跑是 0 筆；
     30 天保留期下**最早到期的是 `w433`，日期 2026-09-17**。
     後續陸續到期：9 月 95 筆、10 月 273 筆、11 月 35 筆。
  2. **約 2026-09-05 ~ 09-08** 再跑一次爬蟲上架（節奏 8～11 天）。
  3. （可選）架構候選 B：頁面模板單一真理源。

## 📅 2026-08-22 工作記錄
- **開工時的異常**：本地工作區停在 8/3、落後 origin/main **61 個 commit**。
  查出原因是 **Google Drive 上有兩份同一 repo 的 clone**：8/10 與 8/21 的工作其實都在
  `法拍物件網頁發布工具\` 那一份做的，而 CLAUDE.md 登記的是 `Queen-of-Auctions\`。
  `publish_new.py` 的 `REPO` 也寫死指向前者。已 pull 追上（474→385 筆物件的差異一併同步）。

- **決策：流標重新排拍「兩個方向一起做」**（8/21 留下待討論的題目）。
  - **(b) 同案號就地更新**（`publish_new.py`）：案號比對到既有物件時不再跳過，
    改比對拍別／開標日／開標時間／底價，有變就就地更新。
    不變 `code`／`short`(tinyurl)／`full`／`id`／照片／經緯度／`school`.`shop`.`traffic`／`name`／導覽鏈；
    更新 `auc`.`date`.`dateISO`.`time`.`price`.`unit`.`status`.`hand`.`detail`.`feat` 與整頁 `index.html`。
    自動刪除 `voice.mp3`／`card.jpg`／`hero.jpg` 讓下游重生。`--no-update` 可退回舊行為。
  - **(a) 保留期 7→30 天**（`purge_expired.py` 預設值）。
  - ⚠️ **關鍵發現：(a) 單獨做會讓情況更糟**。保留期拉長 → 舊物件還在 BASE_ITEMS → 案號比對得到
    → 舊邏輯把新拍別當重複跳過 → 物件頁停在過期的舊拍別 → 被 `delist.js` 隱藏
    → **明明還在拍的物件反而從網站上消失**。也就是「留越久、消失越多」。
    **(b) 是 (a) 的前提**，兩者必須成套。已寫進 CLAUDE.md 警語。

- **實證：站上當時有 24 筆物件資訊已過期**。拿 8/21 那份爬蟲檔用新邏輯 dry-run，
  新物件 0 筆但 24 筆既有物件的拍別／日期／底價已變（例：w395 站上是一拍 8/19 960萬，
  實際已是二拍 9/9 864萬）。這就是舊邏輯「同案號一律跳過」累積下來的黑洞。
  已跑完整產線修好：`publish_new.py`(24) → `fix_city_labels.py`(0) → `burn_price.ps1`(24)
  → `apply_price_images.ps1`(24) → `apply_swipe_nav.py`(24) → `local_voice_batch.py`(24/24)。
  語音 11 分鐘、平均 29 秒/筆、零失敗。線上驗證：24 筆 × 4 項資源（頁面/voice/card/hero）全 200，
  **tinyurl 短網址 301 仍指向原 wNNN，連結沒失效**——這正是本次改動的目的。

- **工作區統一**：舊 clone（936 MB）刪除前五項檢查全過（HEAD 是 origin/main 祖先、未追蹤 0、
  無獨有分支、stash 0、唯一未進版控的檔案已救出），已刪除。
  `local_voice_batch.py` 的 `REPO` 改為 `__file__` 自動定位（原本寫死指向即將刪除的資料夾，
  **本來會把語音生成到錯的地方**）。其餘腳本本來就用 `Path(__file__).parent`，不受影響。

- **品牌素材**：刪 `透明背景.png`（8/2 判定去背不完整、牆面殘留）與 `S__21946379_mirrored.jpg`
  （鏡像版有白邊光暈）；留 `白底.jpg`（`queen.png` 再生來源）與 `S__21946379.jpg`（完整品牌宣傳圖）。
  注意：**從版控刪除不會讓 repo 變小**，兩檔 8/11 已進 git 歷史（`999e7bf`），實益只有工作區乾淨。

- **8/2 備份分支已推回 GitHub**：`backup-old-main-20260802`（`37b8fe4`）。
  查證內容：36 commit、40 檔、作者 `ganggo880`、與現行 main **無共同祖先**，
  是 `beizhong/`.`boai3/` 那種文字代號的更早一代網站。
  原本 GitHub 上已被刪除，只靠這份 clone 的 remote-tracking ref 撐著——
  任何人跑一次 `git fetch --prune` 就會消失。已推成正式分支永久保存。

- **踩到的三個坑（都已解決）**：
  1. `publish_new.py` 的頁面母版寫死讀 `w356/index.html`，但 **w356 只是一般法拍物件**
     （開標日 2026-10-20），30 天保留期下 11/19 就會被 `purge_expired.py` 刪掉 → 下次上架直接 crash。
     已改成 w356 不存在時自動退到現存代號最大的物件頁。
  2. **爬蟲的開標時間常是 `..` 之類的雜訊**，就地更新時會覆蓋掉原本正常的值。
     改成「能解析才採用；不能解析且開標日有變就清空」——清空＝時間未知，
     由 `delist.js` 保守以 17:00 起算，不會把上一拍的舊時間留給新拍別。
  3. **`.gitignore` 寫 `output/` 時，`!output/xxx` 例外規則不會生效**——
     git 不會進入被排除的「目錄」。要改成 `output/*` 才能對單一檔案開例外。
     （為了保存 `price-overlay-preview.html` 才踩到。）

- **上次做到哪**：
  - 三項需求全部完成並推上線；工作區乾淨、與 origin/main 同步（`ed3fa68`）。
  - 資料完整性：385 資料夾 = 385 BASE_ITEMS，voice/card/hero 缺 0。
  - 兩份 clone 已合一，`ai agent\` 下只剩 `Queen-of-Auctions`。

- **下一步**：
  1. **2026-08-28** 跑 `python purge_expired.py --dry-run`（預設已是 30 天），確認後 `--push`。
  2. 下次上架時實地驗證就地更新在「有新物件＋有更新」混合情境下的表現
     （今天只驗證到純更新 24 筆、新物件 0 筆）。
  3. 這個專案還沒有 `agents.md` ＋ `handoff.md`，目前靠本筆記代替。
     今天「本地落後 19 天」的狀況，交接檔的「最後更新者＋電腦名」欄位能第一時間抓到——可考慮補建。

## 📅 2026-08-21 工作記錄
- **今日工作內容**：
  - **清除過期物件**：`purge_expired.py` 清掉下架滿 7 天的 **263 筆**（w054–w490），總覽 474 → 211 筆。
    驗證斷鏈 0、BASE_ITEMS 與資料夾完全一致。
  - **上架 174 筆新物件（w593–w766）**：爬蟲 1355 筆 → 去重後 174 筆，總覽 211 → **385 筆**。
    台南 66、高雄 59、嘉義 28、屏東 21；155 筆有實景照、19 筆用地圖。
  - **語音 174 筆**：本機 GPU，85 分鐘、平均 29 秒／筆、零失敗。
  - 產線其餘步驟照 CLAUDE.md 順序跑完：`fix_city_labels.py`（修 14 筆嘉義市標籤、同步 25 頁導覽名稱）
    → `burn_price.ps1` → `apply_price_images.ps1` → `apply_swipe_nav.py`。
- **本批的重要發現：174 筆裡有 124 筆是「剛清掉的物件流標後重新排拍」**：
  一拍→二拍 38、二拍→三拍 52、三拍→四拍 12、同拍別改期 18 等；真正全新的只有 50 筆。
  **副作用**：同一物件每流標一次就換新代號與新短網址，先前發出去的連結會失效。
  若對招商有影響，可考慮 `purge_expired.py --days 30` 拉長保留期，
  或讓產線支援「同案號更新既有物件」而非新建。**尚未決定，待討論。**
- **踩到的三個狀況（都已解決）**：
  1. `w717` 爬蟲同時缺照片網址與經緯度 → 頁面破圖。以 Nominatim 反查座標
     （門牌查不到就退到路段層級）補生成 `map.jpg`。**下次再遇到可循此模式處理。**
  2. push 被拒 `non-fast-forward`，一度疑似 8/2 那種遠端被覆蓋事故。查證後是
     **雲端 GitHub Actions 工作流自己醒來**，凌晨用 CPU 生成了 w593–w596 的語音。
     共同祖先正常，以 `git merge -X theirs` 保留遠端已發布的那 4 筆、其餘用本機版，
     **沒有 force push**。
  3. 雲端與本機重複做工：`generate_voice.yml` 觸發條件是 push 到 main 且 `**/index.html` 有變動。
     上架時必然觸發。之後不會再衝突（工作流只處理缺語音的物件，現在 385 筆全有）。
- **上次做到哪**：
  - 兩項需求（清除＋上架）全部完成並推上線，GitHub Pages 已驗證：
    線上 385 筆、全部帶 thumb/hero、嘉義市標籤零殘留、語音與地圖皆 200。
  - 工作區乾淨，與 origin/main 同步。
- **下一步**：
  - 決定「流標重新排拍」要不要改變處理方式（保留期 or 同案號更新）。
  - 約一週後再跑 `python purge_expired.py --dry-run`：本批開標日多在 2026/09（115 筆）。
  - `104woo-assets` 4 張品牌素材仍在版控中，待確認是否保留。

## 📅 2026-08-10 ~ 08-11 工作記錄（跨夜，18 個 commit）
- **今日工作內容**：
  - **照片燒上拍賣底價（主要需求）**：先做三方案樣式範本供確認，選定「總覽卡片＝右下角價格牌、物件頁大圖＝底部漸層大字」，
    並採「燒進圖片檔」而非 CSS 疊字，讓 LINE／FB 分享縮圖也看得到價格（`og:image` 改指向 `hero.jpg`）。
    - 新增 `burn_price.ps1`（PowerShell + .NET GDI+，本機無 Python 時也能跑）與 `apply_price_images.ps1`
    - 尺寸依版面實際比例訂：`card.jpg` 660×340、`hero.jpg` 1422×840，`object-fit:cover` 才不會裁掉價格
    - `.card .photo` 由固定 `height:170px` 改為 `aspect-ratio:33/17`（卡片寬度會從 330 變到 660，原本會裁掉價格牌）
    - 卡片字級最後定為 24px（試過 32px、40px）
  - **上架 126 筆新物件（w467–w592）**：爬蟲 1437 筆 → 去重後 126 筆。總覽 348 → **474 筆**。
  - **語音 130 筆**：本機 GPU 生成，71 分鐘、平均 33 秒／筆、零失敗。
  - **手機滑動切換物件**：新增 `104woo-assets/swipe-nav.js`（474 頁共用）＋ `apply_swipe_nav.py`。
    左滑→上一個、右滑→下一個，對應導覽列位置。已用真實檔案模擬觸控實測通過。
  - **修正嘉義市標籤錯置（26 筆）**：新增 `fix_city_labels.py`，兩段皆可重複執行。
  - **工作區清理**：刪 17 個孤兒資料夾（8/2 遠端被舊歷史覆蓋的殘骸，5.9 MB）、`k2rojm/intro.mp3`；
    74 個 mattpocock skills 刪除入版控；`104woo-assets` 4 張品牌素材改為 commit 保存（`白底.jpg` 是 `queen.png` 的去背來源）。
- **環境重建（本機語音環境曾遺失）**：
  - `C:\Users\ken\voxcpm` 不存在了 → 重建於 `C:\Users\ken\.venvs\voxcpm`（torch 2.11.0+cu128、voxcpm 2.0.3，RTX 5070 / sm_120 驗證可用）
  - 另建 `C:\Users\ken\.venvs\fapai104` 給爬蟲產線（requests／openpyxl／bs4／Pillow／lxml）
  - ffmpeg 以 winget 安裝（9.0）；`local_voice_batch.py` 原本寫死 8.1.2 路徑，改為萬用字元比對
  - **venv 一律建在本機碟，不要放 Google Drive**（會同步上萬個檔案）
- **重要發現：爬蟲深度 20 頁就夠**：
  `--max-pages 60` 多跑一小時、多抓 782 筆，但**新物件 0 筆**。2219 筆只含 344 個不重複案號
  （同一物件平均被 6.4 位仲介重複刊登），且全部已在站上。已寫進 CLAUDE.md 附對照表。
- **上次做到哪**：
  - 四項需求全部完成並推上線，GitHub Pages 已驗證生效（474 筆、`swipe-nav.js` 200、`card.jpg` 與本地一致）。
  - 工作區與 origin/main 完全同步，無未 commit 變更。
- **下一步**：
  - 下週跑 `python purge_expired.py --dry-run`：這批物件開標日很近（多為 8/11、8/12），
    依「開標＋3 小時」會陸續自動下架，滿 7 天後可清除。
  - 新物件上架後的完整順序已寫進 CLAUDE.md，關鍵是
    **`fix_city_labels.py` 必須早於 `local_voice_batch.py`**（它會改語音講稿）。
  - `publish_new.py` 的母版不含 `swipe-nav.js`，新物件上架後要補跑 `apply_swipe_nav.py`。
  - 待確認：`104woo-assets` 那 4 張素材是否要保留（目前已入版控）。

## 📅 2026-08-03 工作記錄
- **今日工作內容**：
  - 環境同步：解決 CLAUDE.md 本地路徑合併衝突（統一為 `Queen-of-Auctions`），並將本地分支拉到與 origin/main 一致（追上 275 個 commit）。
  - 語音克隆進度盤點：確認仍卡在 384/466，82 筆（w385–w466）待補。上次手動觸發的 workflow run 於 2026-08-02 15:11 UTC 啟動，跑到 6 小時上限被系統終止（21:05 UTC，完成到 w384），之後未再觸發，已閒置約 4.5 小時。
- **上次做到哪**：
  - 語音克隆卡在 384/466，尚缺 82 筆，需重新觸發 workflow 才會繼續。
- **下一步**：
  - 重新觸發 `generate_voice.yml`（`workflow_dispatch`）補完剩餘 82 筆，預估還需 3–4 次（每次約可完成 18–25 筆）。
  - 留意遠端是否再被 force push 覆蓋（備份分支 `backup-old-main-20260802` 仍保留）。

## 📅 2026-08-02 工作記錄
- **今日工作內容**：
  - **架構深化（候選 A）**：把散落四處、三種矛盾版本（3小時／3天／1天）的下架規則，收斂成單一真理源 `104woo-assets/delist.js`（deep module：`isExpired` 純函式 ＋ `applyToPage`）。
    - 新增 `delist.test.html`（10 個斷言、雙擊即跑，10/10 通過）
    - 順手修掉 bug：`time="00:00"` 被當午夜 → 開標日凌晨就下架；改為保守以 17:00 起算
    - 遷移全部 410 張 `wNNN/index.html`，生成器（template／publish_new.py）改薄為只輸出 `data-auction` 屬性＋`<script src>`
    - 新增 `CONTEXT.md` 領域模型（下架判定／物件生命週期／生成器）
    - 舊系統退役：刪除 `publisher.html`、`template.html`；根目錄 `index.html` 改為轉址頁導向 104woo.html（保留 12 個 hash 代號物件頁封存）
  - **品牌人像更新**：發現指定的 `透明背景.png` 去背不完整（牆面殘留），改用 rembg 從 `白底.jpg` 重新乾淨去背；`queen.png`（450×704）與總覽 `QUEEN_IMG`（300×470）同步更新。
  - **上架 56 筆新物件（w411–w466）**：爬蟲 1363 筆 → 案號正規化＋批次去重 → 實際新物件 56 筆（高雄 28、台南 15、嘉義 7、屏東 6）。總覽 410 → 466 筆。
  - **⚠ 遠端異常事件**：推送時發現 GitHub main 被換成另一條**無共同祖先**的舊歷史（40 個檔、hash 代號物件、publisher.html 那套）。今日先前推的 5 個 commit 全不在其上。處理：先建備份分支 `backup-old-main-20260802` 保住舊版，再以 `--force-with-lease` 推回完整版（1425 檔／466 筆物件）。
  - **修復語音工作流**：查出每次 run 都 failure 的原因是最後多餘的「Commit and Push changes」步驟（`sync_voices.py` 已用 GitHub API 逐筆上傳，該步驟的 checkout 必然落後遠端而 non-fast-forward）。已移除該步驟，並在 `sync_voices.py` 加入時間預算（預設 4.5 小時，`VOICE_TIME_BUDGET_SEC` 可調），讓 run 在 6 小時上限前乾淨收工、進度可累積。
- **上次做到哪**：
  - 語音工作流 run `30753780050` 跑滿 6 小時被系統 cancelled，本次生成 **18 筆**語音並已上傳；本地已 pull 同步。
  - 目前仍有 **82 筆**物件缺 `voice.mp3`。
- **下一步**：
  - 重複觸發語音工作流補完剩餘 82 筆：`gh workflow run generate_voice.yml --repo taiwan2531-web/Queen-of-Auctions --ref main`（新版已有分批機制，每次約可完成 18–25 筆，預估再 4 次）。
  - 留意遠端是否再被 force push 覆蓋——需查明是哪個工具／流程在改寫此倉庫（備份分支 `backup-old-main-20260802` 保留中）。
  - 架構報告候選 B（頁面模板單一真理源）尚未執行，可擇期進行。

## 📅 2026-07-24 工作記錄
- **今日工作內容**：
  - 修復 Git 倉庫損毀問題，重新從 GitHub 克隆並連結 `taiwan2531-web/Queen-of-Auctions`，同步全域設定初始化（.gitignore、Obsidian、CLAUDE.md 路徑修正）。
  - 安裝 mattpocock/skills 共 24 個技能至 `.claude/skills/`（重啟後生效，首次執行 `/setup-matt-pocock-skills`）。
  - 104woo.html 下架規則改版：由「拍賣日後 1 天寬限」改為「**開標時刻＋3 小時**」精準自動下架（支援全形冒號時間；無時間以 17:00 保守起算）。
  - 本機安裝 Python 3.12，執行 104woo 爬蟲（嘉義縣/台南市/高雄市/屏東縣，共 1305 筆）。
  - 建立批次上架產線：案號正規化去重（長短格式歸一）→ 193 筆候選去重為 **54 筆真新物件** → 自動生成 w357～w410 物件頁（照片下載/地圖快照、OSM 1 公里機能、tinyurl 短網址、慧瑜講稿）。
  - 重複檢查規則已寫入專案 CLAUDE.md（使用者指示：上架前必先查重）。
  - Commit `babb886` 已推送，雲端 Generate Clone Voice #12 執行中（54 筆克隆語音陸續生成）。
- **上次做到哪**：
  - w357～w410 共 54 筆新物件已上線，總覽 410 筆（在架 296 筆）；3 小時自動下架已生效。
- **下一步**：
  - 確認 54 筆克隆語音全部生成完畢（若工作流超時，下次 push 會自動接續）。
  - 抽查線上頁面：https://taiwan2531-web.github.io/Queen-of-Auctions/w357/
  - 產線腳本 publish_new.py 目前在 scratchpad，可考慮收進「法拍 104」專案永久保存。

## 📅 2026-07-19 工作記錄
- **今日工作內容**：
  - 處理 `n29eyq` 物件圖片 `n29eyq.jpg`，將中段房屋與街道背景完全去除，僅保留法拍女王人物（陳慧瑜）的去背影像。
  - 將中段背景替換為乾淨專業的純白底色，並確保最上方的標題藍條、最下方的聯絡資訊藍條與人物位置完美對齊，不破壞網頁版面。
  - 本機安裝並使用了 `rembg` AI 去背工具，以局部裁切偵測的方式，完美完成了人物去背處理。
- **上次做到哪**：
  - 成功以 AI 局部去背重組方式更新了 `n29eyq/n29eyq.jpg`，並清理了所有暫存的中間檔案，使專案目錄保持乾淨。
- **下一步**：
  - 提交 Git commit 並推送（push）更新至 GitHub 倉庫 `taiwan2531-web/Queen-of-Auctions`，讓線上網頁正式更新。
