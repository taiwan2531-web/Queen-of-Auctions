# 👑 法拍女王網頁發布工具 - 工作記錄

## 📅 2026-08-10 ~ 08-11 工作記錄（跨夜，18 個 commit）
- **今日工作內容**：
  - **照片燒上拍賣底價（主要需求）**：先做三方案樣式範本供確認，選定「總覽卡片＝右下角價格牌、物件頁大圖＝底部漸層大字」，
    並採「燒進圖片檔」而非 CSS 疊字，讓 LINE／FB 分享縮圖也看得到價格（`og:image` 改指向 `hero.jpg`）。
    - 新增 `burn_price.ps1`（PowerShell + .NET GDI+，本機無 Python 時也能跑）與 `apply_price_images.ps1`
    - 尺寸依版面實際比例訂：`card.jpg` 660×340、`hero.jpg` 1422×840，`object-fit:cover` 才不會裁掉價格
    - `.card .photo` 由固定 `height:170px` 改為 `aspect-ratio:33/17`（卡片寬度會從 330 變到 660，原本會裁掉價格牌）
    - 卡片字級最後定為 24px（試過 32px、40px）
  - **上架 126 筆新物件（w467–w592）**：爬蟲 1437 筆 → 去重後 126 筆。總覽 348 → **474 筆**。
  - **語音 130 筆**：本機 GPU 生成，71 分鐘、平均 33 秒／筆、零失敗。
  - **手機滑動切換物件**：新增 `104woo-assets/swipe-nav.js`（474 頁共用）＋ `apply_swipe_nav.py`。
    左滑→上一個、右滑→下一個，對應導覽列位置。已用真實檔案模擬觸控實測通過。
  - **修正嘉義市標籤錯置（26 筆）**：新增 `fix_city_labels.py`，兩段皆可重複執行。
  - **工作區清理**：刪 17 個孤兒資料夾（8/2 遠端被舊歷史覆蓋的殘骸，5.9 MB）、`k2rojm/intro.mp3`；
    74 個 mattpocock skills 刪除入版控；`104woo-assets` 4 張品牌素材改為 commit 保存（`白底.jpg` 是 `queen.png` 的去背來源）。
- **環境重建（本機語音環境曾遺失）**：
  - `C:\Users\ken\voxcpm` 不存在了 → 重建於 `C:\Users\ken\.venvs\voxcpm`（torch 2.11.0+cu128、voxcpm 2.0.3，RTX 5070 / sm_120 驗證可用）
  - 另建 `C:\Users\ken\.venvs\fapai104` 給爬蟲產線（requests／openpyxl／bs4／Pillow／lxml）
  - ffmpeg 以 winget 安裝（9.0）；`local_voice_batch.py` 原本寫死 8.1.2 路徑，改為萬用字元比對
  - **venv 一律建在本機碟，不要放 Google Drive**（會同步上萬個檔案）
- **重要發現：爬蟲深度 20 頁就夠**：
  `--max-pages 60` 多跑一小時、多抓 782 筆，但**新物件 0 筆**。2219 筆只含 344 個不重複案號
  （同一物件平均被 6.4 位仲介重複刊登），且全部已在站上。已寫進 CLAUDE.md 附對照表。
- **上次做到哪**：
  - 四項需求全部完成並推上線，GitHub Pages 已驗證生效（474 筆、`swipe-nav.js` 200、`card.jpg` 與本地一致）。
  - 工作區與 origin/main 完全同步，無未 commit 變更。
- **下一步**：
  - 下週跑 `python purge_expired.py --dry-run`：這批物件開標日很近（多為 8/11、8/12），
    依「開標＋3 小時」會陸續自動下架，滿 7 天後可清除。
  - 新物件上架後的完整順序已寫進 CLAUDE.md，關鍵是
    **`fix_city_labels.py` 必須早於 `local_voice_batch.py`**（它會改語音講稿）。
  - `publish_new.py` 的母版不含 `swipe-nav.js`，新物件上架後要補跑 `apply_swipe_nav.py`。
  - 待確認：`104woo-assets` 那 4 張素材是否要保留（目前已入版控）。

## 📅 2026-08-03 工作記錄
- **今日工作內容**：
  - 環境同步：解決 CLAUDE.md 本地路徑合併衝突（統一為 `Queen-of-Auctions`），並將本地分支拉到與 origin/main 一致（追上 275 個 commit）。
  - 語音克隆進度盤點：確認仍卡在 384/466，82 筆（w385–w466）待補。上次手動觸發的 workflow run 於 2026-08-02 15:11 UTC 啟動，跑到 6 小時上限被系統終止（21:05 UTC，完成到 w384），之後未再觸發，已閒置約 4.5 小時。
- **上次做到哪**：
  - 語音克隆卡在 384/466，尚缺 82 筆，需重新觸發 workflow 才會繼續。
- **下一步**：
  - 重新觸發 `generate_voice.yml`（`workflow_dispatch`）補完剩餘 82 筆，預估還需 3–4 次（每次約可完成 18–25 筆）。
  - 留意遠端是否再被 force push 覆蓋（備份分支 `backup-old-main-20260802` 仍保留）。

## 📅 2026-08-02 工作記錄
- **今日工作內容**：
  - **架構深化（候選 A）**：把散落四處、三種矛盾版本（3小時／3天／1天）的下架規則，收斂成單一真理源 `104woo-assets/delist.js`（deep module：`isExpired` 純函式 ＋ `applyToPage`）。
    - 新增 `delist.test.html`（10 個斷言、雙擊即跑，10/10 通過）
    - 順手修掉 bug：`time="00:00"` 被當午夜 → 開標日凌晨就下架；改為保守以 17:00 起算
    - 遷移全部 410 張 `wNNN/index.html`，生成器（template／publish_new.py）改薄為只輸出 `data-auction` 屬性＋`<script src>`
    - 新增 `CONTEXT.md` 領域模型（下架判定／物件生命週期／生成器）
    - 舊系統退役：刪除 `publisher.html`、`template.html`；根目錄 `index.html` 改為轉址頁導向 104woo.html（保留 12 個 hash 代號物件頁封存）
  - **品牌人像更新**：發現指定的 `透明背景.png` 去背不完整（牆面殘留），改用 rembg 從 `白底.jpg` 重新乾淨去背；`queen.png`（450×704）與總覽 `QUEEN_IMG`（300×470）同步更新。
  - **上架 56 筆新物件（w411–w466）**：爬蟲 1363 筆 → 案號正規化＋批次去重 → 實際新物件 56 筆（高雄 28、台南 15、嘉義 7、屏東 6）。總覽 410 → 466 筆。
  - **⚠ 遠端異常事件**：推送時發現 GitHub main 被換成另一條**無共同祖先**的舊歷史（40 個檔、hash 代號物件、publisher.html 那套）。今日先前推的 5 個 commit 全不在其上。處理：先建備份分支 `backup-old-main-20260802` 保住舊版，再以 `--force-with-lease` 推回完整版（1425 檔／466 筆物件）。
  - **修復語音工作流**：查出每次 run 都 failure 的原因是最後多餘的「Commit and Push changes」步驟（`sync_voices.py` 已用 GitHub API 逐筆上傳，該步驟的 checkout 必然落後遠端而 non-fast-forward）。已移除該步驟，並在 `sync_voices.py` 加入時間預算（預設 4.5 小時，`VOICE_TIME_BUDGET_SEC` 可調），讓 run 在 6 小時上限前乾淨收工、進度可累積。
- **上次做到哪**：
  - 語音工作流 run `30753780050` 跑滿 6 小時被系統 cancelled，本次生成 **18 筆**語音並已上傳；本地已 pull 同步。
  - 目前仍有 **82 筆**物件缺 `voice.mp3`。
- **下一步**：
  - 重複觸發語音工作流補完剩餘 82 筆：`gh workflow run generate_voice.yml --repo taiwan2531-web/Queen-of-Auctions --ref main`（新版已有分批機制，每次約可完成 18–25 筆，預估再 4 次）。
  - 留意遠端是否再被 force push 覆蓋——需查明是哪個工具／流程在改寫此倉庫（備份分支 `backup-old-main-20260802` 保留中）。
  - 架構報告候選 B（頁面模板單一真理源）尚未執行，可擇期進行。

## 📅 2026-07-24 工作記錄
- **今日工作內容**：
  - 修復 Git 倉庫損毀問題，重新從 GitHub 克隆並連結 `taiwan2531-web/Queen-of-Auctions`，同步全域設定初始化（.gitignore、Obsidian、CLAUDE.md 路徑修正）。
  - 安裝 mattpocock/skills 共 24 個技能至 `.claude/skills/`（重啟後生效，首次執行 `/setup-matt-pocock-skills`）。
  - 104woo.html 下架規則改版：由「拍賣日後 1 天寬限」改為「**開標時刻＋3 小時**」精準自動下架（支援全形冒號時間；無時間以 17:00 保守起算）。
  - 本機安裝 Python 3.12，執行 104woo 爬蟲（嘉義縣/台南市/高雄市/屏東縣，共 1305 筆）。
  - 建立批次上架產線：案號正規化去重（長短格式歸一）→ 193 筆候選去重為 **54 筆真新物件** → 自動生成 w357～w410 物件頁（照片下載/地圖快照、OSM 1 公里機能、tinyurl 短網址、慧瑜講稿）。
  - 重複檢查規則已寫入專案 CLAUDE.md（使用者指示：上架前必先查重）。
  - Commit `babb886` 已推送，雲端 Generate Clone Voice #12 執行中（54 筆克隆語音陸續生成）。
- **上次做到哪**：
  - w357～w410 共 54 筆新物件已上線，總覽 410 筆（在架 296 筆）；3 小時自動下架已生效。
- **下一步**：
  - 確認 54 筆克隆語音全部生成完畢（若工作流超時，下次 push 會自動接續）。
  - 抽查線上頁面：https://taiwan2531-web.github.io/Queen-of-Auctions/w357/
  - 產線腳本 publish_new.py 目前在 scratchpad，可考慮收進「法拍 104」專案永久保存。

## 📅 2026-07-19 工作記錄
- **今日工作內容**：
  - 處理 `n29eyq` 物件圖片 `n29eyq.jpg`，將中段房屋與街道背景完全去除，僅保留法拍女王人物（陳慧瑜）的去背影像。
  - 將中段背景替換為乾淨專業的純白底色，並確保最上方的標題藍條、最下方的聯絡資訊藍條與人物位置完美對齊，不破壞網頁版面。
  - 本機安裝並使用了 `rembg` AI 去背工具，以局部裁切偵測的方式，完美完成了人物去背處理。
- **上次做到哪**：
  - 成功以 AI 局部去背重組方式更新了 `n29eyq/n29eyq.jpg`，並清理了所有暫存的中間檔案，使專案目錄保持乾淨。
- **下一步**：
  - 提交 Git commit 並推送（push）更新至 GitHub 倉庫 `taiwan2531-web/Queen-of-Auctions`，讓線上網頁正式更新。
